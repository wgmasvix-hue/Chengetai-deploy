#!/usr/bin/env bash
# ROSERAG platform plugin for ChengetAi Deploy — placeholder.

# shellcheck disable=SC2034  # PLUGIN_* consumed by the CLI after sourcing
PLUGIN_NAME="roserag"
PLUGIN_DESCRIPTION="ROSERAG retrieval-augmented research assistant (coming soon)"
PLUGIN_STATUS="coming-soon"

plugin_deploy() {
    error "The 'roserag' template is not available yet. Currently available: dspace"
}
