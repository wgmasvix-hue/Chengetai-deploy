// ChengetAi Deploy — runtime configuration.
// Edit this file on the web host to point the dashboard at your backend API.
// No rebuild needed: change apiUrl, save, refresh the browser.
//
//   apiUrl : full URL of the backend API, ending in /api
//            e.g. "https://api.yourdomain.co.zw/api"
//                 "http://57.173.127.168:3000/api"
//
// Leave apiUrl empty ("") to auto-use the same host that serves this page
// on port 3000 (only works when the API is on that same host).
window.__CHENGETAI_CONFIG__ = {
  apiUrl: "http://57.173.127.168:3000/api"
};
